# Task 2: 뉴스 헤드라인 감정 라벨링 웹앱

## 목적
Task 1에서 선정된 뉴스 헤드라인을 여러 명의 작업자가 KOTE 44개 감정 라벨로 멀티라벨 라벨링하는 웹앱.
핵심 가치: **라벨링 UX 편의성 + 저장 안정성**. 그 외 기능은 전부 스코프 아웃.

## 스코프 (엄격히 지킬 것)
- 포함: 작업자 선택 → 내 큐 라벨링 → 건별 자동저장 → 진행률 표시
- 제외: 로그인/인증, 관리자 대시보드, 통계 페이지, IAA 계산, 배포 자동화
- 사용 흐름 한 줄 요약: "URL 접속 → 내 이름 선택 → 라벨링 → 자동저장"

## 기술 스택
- Backend: FastAPI
- DB: Supabase (Postgres) — 로컬 개발 시 SQLite fallback 허용하되 스키마 호환 유지
- Frontend: React (단일 페이지, 빌드 간단하게)

## 데이터 모델
### articles
- article_id (PK), headline, press, category, published_at, bucket, model_top5_labels(json), model_top5_probs(json)
- Task 1 산출 CSV를 임포트하는 스크립트 포함할 것

### assignments
- article_id, annotator_id, status(pending/done/skipped)
- 사전 분할 배정: 임포트 시 작업자 명단을 받아 라운드로빈으로 배정 (중복 라벨링 배수 k는 config — 기본 1, 나중에 2~3으로 올릴 수 있게)

### annotations
- id, article_id, annotator_id, labels(json array — 선택된 감정 리스트), is_skipped(bool), skip_reason(nullable), created_at, updated_at
- annotator_id + timestamp는 반드시 기록 (추후 IAA 계산 대비)

### annotators
- annotator_id, name — 간단한 명단 테이블. 첫 화면에서 본인 선택

## 기능 명세
1. **작업자 선택 화면**: 명단에서 본인 이름 선택 → localStorage에 저장, 재방문 시 유지
2. **라벨링 화면**: 본인 큐에서 pending 건을 순서대로 1건씩 제시
3. **건별 즉시 저장**: "저장 후 다음" 버튼 클릭 시 서버 저장 성공 확인 후 다음 건으로 이동. 저장 실패 시 명확한 에러 표시 + 재시도 버튼 (진행 데이터 유실 절대 금지)
4. **스킵**: "판단 불가/뉴스 아님" 등 사유 선택과 함께 스킵 가능
5. **이전 건 수정**: 직전 건으로 돌아가 수정 가능 (전체 히스토리 네비게이션까지는 불필요, 이전/다음 이동이면 충분)
6. **진행률**: 상단에 "내 진행: 137 / 350" 형태 상시 표시
7. **키보드 지원**: Enter=저장 후 다음, 숫자키/단축키로 자주 쓰는 라벨 토글 (구현 부담 크면 Enter만이라도)

## UI 명세 (승부처 — 여기에 공을 들일 것)
### 레이아웃
- 상단: 헤드라인 (크고 읽기 편하게), 언론사/날짜는 작게 보조 표시
- 중앙: 44개 감정 라벨 선택 영역
- 하단: 스킵 / 저장 후 다음 버튼, 진행률

### 44개 라벨 선택 영역 (핵심)
- **선택지 제시 방식** (자유 입력 아님): 라벨을 칩(chip)/토글 버튼으로 표시, 클릭으로 on/off
- **그룹핑**: 긍정/부정/중립·기타 3그룹으로 시각적 구분 (KOTE 라벨의 그룹 매핑은 첫 커밋 전 나에게 확인받을 것)
- **그룹 내 정렬: 가나다순 고정** — 모델 예측 기반 정렬은 anchoring bias 우려로 기본 비활성. 단 config flag(`sort_by_model`)로 전환 가능하게 구조만 잡아둘 것
- **모델 후보 제시**: model_top5를 라벨 영역 위에 "모델 추천" 별도 행으로 표시하되, 본 라벨 그리드와 시각적으로 분리. 이것도 config flag(`show_model_suggestions`)로 on/off — **기본값 off** (gold set 오염 방지, 버킷 B 개선용 라벨링 시에만 켤 예정)
- **선택된 라벨 요약**: 현재 선택된 라벨들을 헤드라인 바로 아래 별도 표시 (44개 중 뭘 눌렀는지 한눈에)
- **검색 필터**: 라벨 이름 타이핑으로 즉시 필터 (선택 사항이지만 권장)
- 라벨 미선택 상태로 저장 시도 시 확인 모달 ("감정 없음으로 저장할까요?")

### 반응형
- 데스크톱 우선이되 모바일에서도 라벨 칩이 터치 가능한 크기로 동작할 것

## 구현 순서
1. 스키마 + CSV 임포트 스크립트 → 2. API (큐 조회/저장/스킵) → 3. 라벨링 화면 UI → 4. 자동저장/에러 처리 → 5. 진행률/키보드
각 단계 완료 시 동작 확인 후 다음 진행. UI는 초안 완성 후 나에게 스크린샷으로 보여주고 피드백 받아 수정.

## 확인 필요 사항 (시작 전 나에게 질문할 것)
- KOTE 44개 라벨의 정확한 한국어 명칭 리스트 및 긍/부/중 그룹 매핑
- Supabase 프로젝트 정보 (URL/key) 또는 로컬 SQLite로 먼저 갈지
- 작업자 명단 및 중복 배수 k
